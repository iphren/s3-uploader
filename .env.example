# Defaults read by generate_upload_link.py. CLI flags override these.
S3_BUCKET=my-upload-bucket
AWS_REGION=eu-west-2
PAGE_URL=https://upload.example.com

# Optional: select a named profile from ~/.aws/credentials
# AWS_PROFILE=default
